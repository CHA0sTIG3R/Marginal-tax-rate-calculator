services:
  tax_data:
    image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/marginal-tax-rate-calculator:latest
    pull_policy: always

    env_file:
      - .env

    ports:
      - "8080:8080"
